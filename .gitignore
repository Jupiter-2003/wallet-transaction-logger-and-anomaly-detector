# Maven build output — never commit compiled classes
target/

# The SQLite database file — this is generated at runtime
wallet.db

# Serialized UserProfile store (anomaly engine state) — generated at runtime
data/

# VS Code workspace settings
.vscode/

# Mac junk
.DS_Store

# Java crash logs
hs_err_pid*
